package net.ferderplays.gui.objects;

import net.ferderplays.*;

public class settingspar extends HuqaloV {

    if(module.has(range))
    {
        private static rangepar {
            class par1 {
                int height = 12;
                int width = this.GUI.getWidth();
            }
            if (rangepar.set()) {

    void update() {
                    this.updateModule.with(range);
                    this.update(range);
                }
}}}

private static keybindpar{

class par2 {
            textarea1 = new TextArea();
            textarea1.height = 12;
            textarea1.width = this.GUI.getWidth();
        }}}