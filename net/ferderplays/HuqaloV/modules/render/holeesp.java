package net.ferderplays.modules.render;

import net.minecraft.block.*;
import net.minecraft.hole;

public class holeesp {
    public holeesp() {
        private static final String Setting long obsidiancolor < color > = this.setHoleType("obsidian").setColor(); this.send(obsidian.color()).toInt(obsidianholecolor);
        private static final String Setting long bedrockcolor < color > = this.setHoleType("bedrock").setColor(); this.send(bedrock.color()).toInt(bedrockholecolor);
    }

    void enable() {
        private static obsidianholecolor {
            int obsidianholecolor = this.get(obsidian.color());
        }

        private static bedrockholecolor {
            int bedrockholecolor = this.get(bedrock.color());
        }
    }
}
